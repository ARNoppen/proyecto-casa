# URL de la API (para desarrollo local suele ser /api por el proxy de Vite)
VITE_API_BASE_URL=/api

# En producción, si el backend está en otro dominio:
# VITE_API_BASE_URL=https://mi-api-desplegada.com/api
